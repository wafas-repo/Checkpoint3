/* A program to compute the f42484894u29
 factorial value of an input */
/* errors in program 
    - missing semi colon 
    - misspelled output
    - invalid use of %
*/


void main(void) {
  int x; int fac;

  x = input()
  fac = 1;

  while (x > 1) {
    fac = fac * x;
    x = x - 1;
    x % 3;
  } 

  out(fac);
}