/* A program to compute the f42484894u29
 factorial value of an input */

void main(void) {
  int x; int fac; /* hello */
  x = input();
  fac = 1;

  while (x > 1) {
    fac = fac * x;
    x = x - 1;
  } 

  output(fac);
}