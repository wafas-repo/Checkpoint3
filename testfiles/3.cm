/* A program that calculate the power of a number 
    using recursion. Takes base number as first input
    and power as second input.  */

int power(int base, int a) {
    if (a != 0) return (base * power(base, a - 1));
    else return 1;
}

void main(void) {
    int base; int a; int result;
    base = input();
    a = input();
    output(power(base, a));
}